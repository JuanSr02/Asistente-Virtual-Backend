# =======================
# App Config
# =======================
app.name=Asistente Virtual
server.port=8080
# =======================
# Database Config (Supabase)
# =======================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver
# =======================
# JPA / Hibernate
# =======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Silencia los logs SQL de Hibernate
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=ERROR
logging.level.org.hibernate.type.descriptor.sql=ERROR
# =======================
# Supabase JWT Config
# =======================
supabase.url=${SUPABASE_URL}
supabase.jwt.secret=${SUPABASE_JWT_SECRET}
supabase.serviceRole.key=${SUPABASE_SERVICE_ROLE_KEY}

# HOST DE BREVO
spring.mail.host=smtp-relay.brevo.com
# PUERTO CRÍTICO: USA 2525 (Evita el bloqueo de Render)
spring.mail.port=2525
# TUS CREDENCIALES DE BREVO
# Ojo: El usuario suele ser el email con el que te logueas en Brevo, no necesariamente el remitente
spring.mail.username=juanma2002123@gmail.com
spring.mail.password=xsmtpsib-a54056934eaef622e97d247483e4614ef8e61025ea9beee42c472e03db2526ca-EoIefXUaKZW2lSK9
# SEGURIDAD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
# TIMEOUTS (Mantenlos altos por si acaso)
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000

# =======================
# Multipart Files
# =======================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
# =======================
# Jackson
# =======================
spring.jackson.default-property-inclusion=non_null
# En produccion, SOLO el dominio real
cors.allowed-origins=https://asistente-virtual-unsl.vercel.app,http://localhost:3000
# =======================
# Reglas de Negocio Académicas
# =======================
# Duración de la regularidad (2 años + 9 meses según ordenanza vigente)
app.reglas-academicas.regularidad-anios=2
app.reglas-academicas.regularidad-meses=9